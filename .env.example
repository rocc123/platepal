# Public browser vars. Copy to .env — never commit real values.
# From Supabase: Connect, or Project Settings → API Keys.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional. Food search uses USDA DEMO_KEY without this.
VITE_USDA_API_KEY=
